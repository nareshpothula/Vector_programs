#include<stdio.h>
struct st
{
int rollno;
char name
float marks;
};
main()
{
char 

if(strcmp((s1[i].name,s1[i+1].name)>0))
{
strcpy(temp1,s1[i].name);
strcpy(s1[i].name,s1[i+1].name);
strcpy(s1[i+1].name,temp1);
}

